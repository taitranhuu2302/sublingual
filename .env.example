# Shared backend host/port for Python server and Electron main process.
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8765

# Renderer (Vite) variables must be prefixed with VITE_.
VITE_BACKEND_HOST=127.0.0.1
VITE_BACKEND_PORT=8765

