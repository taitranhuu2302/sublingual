services:
  translate:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    restart: unless-stopped
    ports:
      - "3333:3333"
    volumes:
      - ../models:/app/models
    env_file:
      - ../.env
